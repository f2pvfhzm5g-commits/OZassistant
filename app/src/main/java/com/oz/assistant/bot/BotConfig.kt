package com.oz.assistant.bot

object BotConfig {

    // =========================
    // 🔁 ОБЩАЯ СКОРОСТЬ БОТА
    // =========================

    // Минимальная пауза между ЛЮБЫМИ действиями бота
    // Если бот слишком дёрганый — увеличь (1000–1500)
    // Если слишком медленный — уменьши (600–800)
    var globalActionDelayMs: Long = 800


    // =========================
    // 🧭 НАВИГАЦИЯ ПО ЭКРАНАМ
    // =========================

    // Ждать после нажатия вкладки "Склады"
    var afterOpenWarehousesMs: Long = 1500

    // Ждать после нажатия кнопки "Записаться" у склада
    var afterClickRecordMs: Long = 1500


    // =========================
    // 📅 КАЛЕНДАРЬ
    // =========================

    // Ждать после клика по дате
    var afterPickDateMs: Long = 1200


    // =========================
    // ⏰ ВРЕМЯ
    // =========================

    // Ждать после выбора времени смены
    var afterPickTimeMs: Long = 1200


    // =========================
    // 📜 СКРОЛЛЫ
    // =========================

    // Ждать после любого скролла списка
    var afterScrollMs: Long = 1000
}
